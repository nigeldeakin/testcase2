#command-timeout: 3
#no-response-timeout: 3
build:
  docker: true
  box: alpine
  steps:
    - script:
        name: Say hello
        code: echo hello
  after-steps:
    - script:
        name: An after step 2
        code: |
          sleep 5000
          echo sleep finished
    - script:
        name: An after step 2
        code: |
          sleep 5000
          echo sleep finished

